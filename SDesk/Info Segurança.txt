Informações sistema

Usuário SQL: pi_grp1
Senha: C0c@C0l@


E-mail: pigrp1.suporte@gmail.com
Senha: C0c@C0l@

bibliotecas Python
Flask
Flask_sqlalchemy
Flask_login
Dotenv
Pyodbc
Pandas


testes
USE SistemaChamados;
GO

-- 1. Ver TODOS os schemas e seus donos
SELECT 
    s.name AS SchemaName,
    dp.name AS OwnerName,
    dp.principal_id
FROM sys.schemas s
JOIN sys.database_principals dp ON s.principal_id = dp.principal_id
ORDER BY dp.name;

-- 2. Ver se o usuário pi_grp1 ainda existe no banco
SELECT 
    name,
    principal_id,
    type_desc,
    authentication_type_desc
FROM sys.database_principals 
WHERE name = 'pi_grp1';

-- 3. Ver se o login existe no servidor
USE master;
GO
SELECT 
    name,
    principal_id,
    is_disabled,
    type_desc
FROM sys.server_principals 
WHERE name = 'pi_grp1';